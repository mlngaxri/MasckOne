import assert from 'node:assert/strict'; import { readFileSync } from 'node:fs'; const read=(p)=>readFileSync(new URL(p,import.meta.url),'utf8');
const html=read('./src/index.html'),js=read('./src/app.js'),css=read('./src/styles.css'),build=read('./build.mjs'),pkg=JSON.parse(read('./package.json')),brand=read('./public/brand/brand-mark.svg'),mono=read('./public/brand/brand-mark-mono.svg'),favicon=read('./public/favicon.svg'),og=read('./public/brand/og-source.svg'),social=read('./public/brand/social-avatar-source.svg');
assert.equal(pkg.scripts?.test,'node test.mjs'); assert.equal(pkg.scripts?.prebuild,'npm test'); assert.equal(pkg.scripts?.build,'node build.mjs'); assert.ok(build.includes("'app.js'")&&build.includes("cp('public','dist'")); assert.ok(html.includes('rel="icon" href="/favicon.svg"'));
for(const a of [brand,favicon,og,social]){assert.ok(a.includes('dominant-field-insert-v3'));assert.ok(!/<(?:linearGradient|radialGradient|filter|mask)\b/.test(a));} for(const a of [brand,favicon,social]) assert.ok(a.includes('#314f38')&&a.includes('#1d211f'));
assert.equal((brand.match(/<path\b/g)||[]).length,2); assert.equal((mono.match(/<path\b/g)||[]).length,2); assert.ok(mono.includes('dominant-field-insert-v3')&&!brand.includes('<rect'));
for(const retired of ['h18v216','M142 20h18','M40 72C40 40 65 20 96 20h16','M144 20h16','M40 72C40 40 65 20 96 20h10','M150 20h10','M40 72C40 40 65 20 96 20L106 20','M150 20L160 20']) assert.ok(!brand.includes(retired)); for(const t of ['M44 76C44 44 68 24 100 24H150','M172 24H180']) for(const a of [brand,favicon,og,social]) assert.ok(a.includes(t));
assert.ok(106/8>=10,'dominant field and insert must not regress to equal bars'); assert.ok(og.includes('Development preview. No performance or availability claim.'));
const ids=new Set([...html.matchAll(/\sid="([^"]+)"/g)].map(m=>m[1])); for(const m of html.matchAll(/\shref="#([^"]+)"/g)) assert.ok(ids.has(m[1])); const stage=html.match(/<div[^>]*\bclass="product-stage"[^>]*>/)?.[0]??'';assert.ok(stage.includes('role="img"')&&stage.includes('aria-label=')); const status=html.match(/<[^>]*\bid="access-status"[^>]*>/)?.[0]??'';assert.ok(status.includes('role="status"')&&status.includes('aria-live="polite"')&&status.includes('aria-atomic="true"')); const notify=html.match(/<button[^>]*\bid="notify"[^>]*>/)?.[0]??'';assert.ok(notify.includes('aria-describedby="access-status"')&&html.includes('>Check early access</button>')&&!html.includes('Join early access')); assert.ok(js.includes('button.disabled=true')&&js.includes("button.textContent='Early access not open'")&&!js.includes('opening later')&&js.includes('No signup or availability is implied')); for(const t of ['fetch(','XMLHttpRequest','WebSocket'])assert.ok(!js.includes(t)); assert.ok(!/\b(?:one|1|60)\s*[- ]?\s*(?:min(?:ute)?s?|sec(?:ond)?s?)\b/i.test(html)); for(const t of ['being engineered','Final service geometry remains subject to engineering validation.','No performance or availability claim is implied by this preview.'])assert.ok(html.includes(t));for(const t of ['prefers-reduced-motion:reduce','prefers-contrast:more','forced-colors:active','@media(max-width:420px)',':focus-visible','.skip:focus'])assert.ok(css.includes(t)); console.log('Masck One web workspace tests passed');
