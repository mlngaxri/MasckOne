name: Retention V40 clearance corner binding

on:
  push:
    branches: [sol-high/retention-quick-release-20260909]
    paths:
      - "src/masck_one/retention_quick_release_tactile_v3[0-9].py"
      - "src/masck_one/retention_quick_release_tactile_v40.py"
      - "tests/test_retention_quick_release_tactile_v40.py"
      - ".github/workflows/retention-v40-clearance-corner-binding.yml"

permissions:
  contents: read

concurrency:
  group: retention-v40-clearance-corner-binding-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clearance-corner-binding:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout exact retention head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install project
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Compile V40
        run: python -m py_compile src/masck_one/retention_quick_release_tactile_v40.py
      - name: Verify clearance-corner collision authority binding
        run: python -m pytest -q tests/test_retention_quick_release_tactile_v40.py

# Sampled digital B-rep provenance gate only. This does not establish continuous
# swept-volume clearance, manufactured tolerance closure, strength, fatigue,
# service force/time, wet-use, wear, contamination, comfort, or physical validation.
